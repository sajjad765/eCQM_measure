<%#This partial can be used by dashboard and src... could instead use view directly though it's not 'the rails way'? render :file => '<replace with relative/absolute path>' %>
<%= render partial: 'vendors/index' %>
